doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title ExpressJs | form output
    body 
    p Name : #{data.name}
    p Password :#{data.password}
    p Email:#{data.email}
    p Gender:#{data.gender}
    p City :#{data.city}